#!/bin/bash

# Add the Yazi launcher (opens yazi in a themed kitty window) to machines that
# were installed before it existed. Fresh installs get it via
# install/desktop/applications.sh, which globs every applications/*.sh.
source $OMAKUB_PATH/applications/Yazi.sh
